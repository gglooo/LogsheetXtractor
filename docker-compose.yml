# Image names match GHCR tags published by .github/workflows/docker-image.yml
# Override via .env (see .env.example): LOGSHEETXTRACTOR_IMAGE, LOGSHEETXTRACTOR_TAG
services:
  backend:
    image: ${LOGSHEETXTRACTOR_IMAGE:-ghcr.io/grp-bork/logsheetxtractor}-backend:${LOGSHEETXTRACTOR_TAG:-latest}
    build:
      context: .
      dockerfile: LogsheetXtractor.Solution/LogsheetXtractor.API/Dockerfile
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
    volumes:
      - ./app_data:/app/app_data
      - ./credentials:/app/credentials

  frontend:
    image: ${LOGSHEETXTRACTOR_IMAGE:-ghcr.io/grp-bork/logsheetxtractor}-frontend:${LOGSHEETXTRACTOR_TAG:-latest}
    build:
      context: ./web
    ports:
      - "3000:80"
    environment:
      - BACKEND_URL=http://backend:8080
    depends_on:
      - backend

  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    ports:
      - "8978:8978"
    volumes:
      - ./app_data:/data
      - ./cloudbeaver:/opt/cloudbeaver/workspace
